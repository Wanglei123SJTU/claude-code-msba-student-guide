# Analysis Runner

Run the full Assignment 1 analysis pipeline from the project root:

```powershell
& 'D:\Anaconda3\python.exe' src/run_analysis.py
```

This reads `inputs/assg1.csv` and writes all tables, charts, and diagnostics to `outputs/`.
